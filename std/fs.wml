let open = $file_open
and read = $file_read
and read_to_end = $file_read_to_end
and write = $file_write
and write_all = $file_write_all
and flush = $file_flush
and RO = 0
and WO = 1
and AO = 2
and RW = 3
and RA = 4 in 
    export {
        open, read, read_to_end, write, write_all, flush, RO, WO, AO, RW, RA
    }